

## Plan: Update AboutView.tsx

1. Remove the illustration placeholder card entirely
2. Replace body text with the corrected copy, with each paragraph as its own `<p>` tag
3. Add final paragraph with two links: "She Builds" → https://shebuilds.lovable.app/ and "Tara Lejermalm" → https://designingtara.com/ (open in new tab, styled with primary color)

### File changed
- `src/pages/AboutView.tsx`

